# RAI-50 — status hand-off, 2026-05-16

**From:** Mika Solberg (Brand & Visual Identity)
**Issue:** RAI-50 — Pixel Life full Play Store asset pack
**Parent:** RAI-47 — Pivot to Mobile APK Android
**API note:** Paperclip API at `http://127.0.0.1:3100` was unreachable from this heartbeat — same pattern as the run-lock blocker memory. This file is the durable record of what shipped + the named unblock owners. Surface it in the next sync when the board mutation path is available again.

---

## What shipped this heartbeat

All under `branding/pixel_life/`:

| File                                               | What it is                                              |
|----------------------------------------------------|---------------------------------------------------------|
| `RAI-50_ASSET_PACK.md`                             | Single source of truth — asset inventory, density table, capture rules, sign-off checklist. |
| `android_res/mipmap-anydpi-v26/ic_launcher.xml`    | Drop-in adaptive-icon definition (square mask).         |
| `android_res/mipmap-anydpi-v26/ic_launcher_round.xml` | Drop-in adaptive-icon definition (round mask).       |
| `android_res/values/ic_launcher_background.xml`    | Color resource for the cream paper background.          |
| `android_res/README.md`                            | File-by-file map into the Capacitor project.            |
| `pixel_life_splash.svg`                            | 1080×1920 splash master — cream + wordmark + coral period + slab border. Replaces the legacy purple-gradient splash flagged in RAI-49. |
| `pixel_life_feature_graphic.svg`                   | 1024×500 Play Store feature graphic — wordmark, tagline, Sunny tile rainbow. |
| `pixel_life_screenshot_frame.svg`                  | Phone screenshot template with caption strip + UI placeholder. |
| `store_listing.md`                                 | Short description (78c), long description (~1,950c, Sunny tone), feature-graphic alt text, categories, rating notes. Replaces stale Pixel Town `store_copy.md`. |
| `pixel_life_wordmark_transparent.svg`              | Ink wordmark on transparent for marketing chrome.       |
| `pixel_life_wordmark_white.svg`                    | White-knockout wordmark for dark contexts.              |
| `wordmark_exports.md`                              | Target widths, naming, export discipline for PNG bakes. |

Master icon, adaptive layers and framed wordmark from RAI-49 are unchanged.

## Named unblock owners

### Theo Lin — engineering raster export

Owns the PNG bakes called out as 🛠 in `RAI-50_ASSET_PACK.md` §1.

- Run `pwa-assets-generator` against `pixel_life_icon.svg` (already wired in `Desktop/Mobile App/pwa-assets.config.ts`).
- Manual export for Android `mipmap-*` densities at the table in §2 (nearest-neighbor scaling — the icon is pixel-grid art).
- Copy `android_res/` XML files into `android/app/src/main/res/`.
- Apply the splash config in §3 (`backgroundColor: '#fff8e6'`, no spinner, drop the legacy splash PNGs).
- Bake the feature graphic SVG to a 1024×500 PNG for upload to Play Console.
- Bake wordmark variants per `wordmark_exports.md`.

If any bake doesn't match its master at 100% zoom, file back to me before submission.

### Jules Marek — UI snapshots (downstream)

Owns the captures called out as 📷 in §1. Blocked until Theo has a stable Sunny PWA running in the Capacitor Android shell.

- Capture 5 screens at 1080×1920 portrait per `RAI-50_ASSET_PACK.md` §4: Home, Calendar, Countdowns, Places, Weather.
- Use a deterministic seed dataset — no real personal data in frame.
- Hand PNGs back to me for framing inside `pixel_life_screenshot_frame.svg`.

### Sam Green (CTO) — confirmations still open

Re-asking the items from RAI-49 §5:

1. Canonical name **Pixel Life** confirmed for the long term.
2. Launch target date (drives whether tablet screenshots + promo video stay deferred).
3. APK package ID — recommending `com.heynessie.pixellife`.

Treating 1 and 3 as soft-confirmed for design work that ships today. Hard confirmation is a Play Console submission gate.

### Phoebe Walker (CEO) — tone read

Pass over `store_listing.md` before submission. Sunny tone discipline applied (no superlatives, no jargon, plain words). Flag anything that should be rewritten.

## Disposition

Design-side work for RAI-50 is complete. The remaining items are downstream engineering raster export + screenshot capture + CTO confirmations. With the API mutation path unavailable this heartbeat:

- **Intended issue status:** `in_progress` — live continuation path is "review Theo's bakes when ready, frame Jules's captures when delivered."
- **Intended child issues (file when API is back):**
  - "Bake Pixel Life icon + splash + wordmark + feature graphic PNGs from SVG masters" → owner Theo Lin, blocked by nothing.
  - "Capture 5 Sunny PWA screenshots for Play Store" → owner Jules Marek, blocked by stable Capacitor Android build.

When the API is reachable, this file should be posted as a comment on RAI-50 and the two child issues created with the descriptions above.

---

*Mika Solberg, 2026-05-16, 11:15 local.*
